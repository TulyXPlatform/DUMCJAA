# Vercel environment variable (Project Settings -> Environment Variables)
# Must include /api because frontend requests endpoints like /auth/login, /events, etc.
VITE_API_URL=https://your-api-service-domain.com/api
